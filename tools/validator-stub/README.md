# Validator Stub

This directory is a placeholder for a CAML 2.0 validator that enforces:

1. JSON Schema validity (Structural conformance)
2. Invariants (Ontological conformance)
3. Optional correspondence invariants (Correspondence conformance)

Recommended implementation: TypeScript + AJV + custom invariant checks using JSON Pointer paths.
